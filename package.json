{
  "name": "cultivo-flora",
  "version": "1.0.0",
  "private": true,
  "description": "Aplicación local-first para el armario de cultivo Flora",
  "scripts": {
    "web:sync": "node scripts/sync-web.mjs",
    "android:sync": "node scripts/sync-web.mjs && cap sync android",
    "android:open": "cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.0",
    "@capacitor/core": "^8.5.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.0"
  }
}
